Execute docs/add-station-catalog-search.md (read fully; §numbers are its). You are ONLY an orchestrator: never write or edit code/docs; plan, decompose, delegate ALL implementation via the Task tool to expert subagents, review results (diff+build/test), reject+re-delegate until green, integrate. Techniques: expert agents live in .claude/agents/ — use them; Phase 0 bootstraps the NEW files (data-engineer, app-integration-engineer, docs-engineer, qa-auditor, design-reviewer, perf-auditor) mirroring existing frontmatter (name, one-line description, tools, role+deliverables+file ownership in body, NO model key); delegate small verifiable slices; run non-overlapping lanes in PARALLEL; adversarial verification wave per lane; implementer ≠ reviewer ≠ auditor. Agents: spec-architect — inventory, CAT-01..18 rows in docs/acceptance-matrix.md, contracts first (StationCatalogEntry, StationCatalogQuery, ICatalogProvider), pre-seed §11 defaults as decisions, baseline 24 suites green; data-engineer — §5 pipeline export+validation (data/output/app-catalog.json, Working-only, deduped, tag precomputed, collections country=Internet), XLSX README-tab rewrite, data/README; core-engineer — DialShift.Core only: StationCatalogEntry+StationCatalogQuery (§7.2, pure, no I/O), Station.Notes nullable JsonIgnore WhenWritingNull, Settings.Version stays 1 (QA-N3), old files load; app-integration-engineer — csproj copy (Content, PreserveNewest, Exists condition, CopyToPublishDirectory, NOT AvaloniaResource), ICatalogProvider/CatalogProvider (§5.2: AppContext.BaseDirectory, DIALSHIFT_CATALOG_PATH override, missing/corrupt→degraded+warning, never crash), DI, verify win-x64+osx-arm64 publish and .app/zip contain parseable JSON; ui-engineer — §6 dialog (search+overlay results with Name·City·FM·Country, five dynamic filters Country/City/Type/Genre/Language, detail pane with notes+votes+logo/monogram, select fills Name/Tag/Url+Notes, debounce via LatestValueDispatcher, empty/no-match/no-catalog states, keyboard Up/Down/Enter/Esc, automation names, VM property names stable so HS-02 stays green, edit mode = plain form, search focused on open in Add mode); test-engineer — Catalog suite (§8) + UiViewModels CAT checks, keep all suites green; release-engineer — CI both OSes, JSON in both artifacts, no new NuGet deps, no Settings.Version bump anywhere; docs-engineer — README, matrix status, decisions D58+, data/README in the SAME change as code; qa-auditor — fresh-context adversarial diff review (correctness/requirements only), dead-code grep, version-bump audit, per-lane CAT verification; design-reviewer — QG-03 audit (theme, spacing, no clipped text at 780×650, honest states, no jank); perf-auditor — §9 budgets measured (≤5000 entries, load <50ms, query <10ms). Sequence: Phase 0 spec-architect alone (new agent files+matrix+contracts; gate: 24 suites green); then PARALLEL lanes data/core/integration/ui (test-engineer joins from Phase 2); Phase 5 verification wave (test+qa+design+perf, diff-only) once 1–4 land; Phase 6 docs+release; fix loop max 3 rounds then escalate. Gates: real dotnet build -warnaserror + test output pasted as evidence per phase; Phase 5 closed only when CAT-01..18 all GREEN; adopt §11 defaults, record decisions, do not stop to ask. Global: branch feature/add-station-catalog-search off main (create it if missing), commit freely, push ONLY private remote git@github.com:spyroskotsakis/dialshift-dev.git; never origin/upstream (tsiger/DialShift read-only); no PRs; never bump Settings.Version; YAML stays the only station-data source; leave NO dead code (delete, never comment-out); docs updated in the SAME change as code; test→fix→retest, never test→report; you run on macOS so Windows smoke/clean-machine checks can't run here — implement + CI, list remaining native checks. Final report: per-agent summaries, real build/test/publish excerpts, CAT matrix status, remaining native checks, decisions, git log --stat.
