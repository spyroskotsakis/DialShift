Execute docs/single-codebase-refactor.md then docs/schedule-timezone-research.md (read fully). You are ONLY an orchestrator: never write or edit code/docs; plan, decompose, delegate all implementation via the Task tool to expert subagents, review results (diff+build/test), reject+re-delegate until green, integrate. Techniques: expert agents are pre-defined in .claude/agents/ — use them; delegate small verifiable slices; run non-overlapping lanes in PARALLEL; adversarial verification wave per lane. Agents: spec-architect — behavior inventory → docs/acceptance-matrix.md, characterization-test plan, contracts first (IPlaybackEngine, IClock/IMonotonicClock, platform interfaces), QA-B1..B4/QA-N1..N9 tracking; core-engineer — DialShift.Core: PlaybackCoordinator §5 (lifetime+linked CTS, generation checks, one SemaphoreSlim gate never held across await, per-state event acceptance), Core purity §6, then Phase 2 timezone Core (ScheduleEntry.TimeZone; ResolveZone null→zero-conversion QA-B1, trim, invalid→null+log; ToComputerLocal ambiguous→earlier daylight QA-B3, invalid→first-valid QA-N7; SpecifyKind(Unspecified)+3-arg ConvertTime QA-B2, DoW in entry zone; Conflicts normalized resolved-Id QA-N5; no Version bump QA-N3; localZone through ScheduleSession; per-day DST memo QA-N4); playback-engineer — LibVlcPlaybackEngine + MacAvPlayerPlaybackEngine (AVPlayer P/Invoke, arm64 ABI, §7.8 criteria), three §7.3 spikes (AVPlayer two checkpoints, 'audio once' fails; SystemEvents compile-level without -windows TFM; NSWorkspace wake 5 criteria else timer-gap fallback), media corpus §11; platform-engineer — IStartupRegistration (transactional status), ISystemPowerEvents, IFileRevealService (ArgumentList only), Windows+macOS impls, single-instance hardening (per-user pipe, CurrentUserOnly, 4-16KB input, timeouts, versioned protocol, lock in canonical data dir, validate macOS socket perms), canonical data dirs; ui-engineer — DialShift.App Avalonia views/viewmodels/dialogs, tray rule (create TrayIcon+NativeMenu once, mutate Items in place, never reassign Menu/SetIcons), timezone ComboBox ('Local time' sentinel=null, IANA via TryConvertWindowsIdToIanaId QA-B4), zone+next-fire row labels QA-N6, UP NEXT zone label; release-engineer — GitHub Actions windows+macos matrix (compile+test+publish), .app Info.plist+LSUIElement no VLC dylibs, icons, README/notices, retire WPF+DialShift.Mac, update slnx; test-engineer — DialShift.Tests: characterization tests, three-level smoke replacing SmokeChecks, second instance ACTIVATES first, all §5.2 timezone cases (differing-zone ambiguous, cross-midnight, non-hour offsets, invalid id, dedup). Sequence: spec-architect first (inventory+matrix+contracts); then fan out parallel lanes where files don't overlap — core-engineer, playback-engineer (spikes first), platform-engineer, ui-engineer, test-engineer; release-engineer once DialShift.App exists; verify each lane vs matrix pre-accept; re-delegate failures, never fix yourself. Phase gate: Phase 2 only after Phase 1 builds/tests/publishes green; Phase 2 UI targets DialShift.App (WPF retired). Global: work on a NEW branch, commit freely, push ONLY to private remote git@github.com:spyroskotsakis/dialshift-dev.git; never push origin/upstream (tsiger/DialShift, read-only); no PRs; verify with real dotnet output; line refs historical; you run on macOS, so Windows smoke, SystemEvents runtime, win-x64 packaging and clean Apple-Silicon tests can't run here — implement + CI and list remaining native checks; adopt §13/§7.2/§10 defaults; record decisions. Quality gates (always): leave NO dead code (retire fully, never comment-out); keep docs current as you work (README, NOTICES, briefs, matrix); ship a clean, polished app with the best UI/UX; test+fix until PROD-READY — every matrix row green, native smokes + clean-machine pass before any phase is done. Final report: per-agent summaries, real build/test/publish excerpts, remaining native checks, decisions, git log --stat.